{
  "name": "gerenciador-de-tarefas",
  "version": "1.0.0",
  "description": "API REST simples em Node.js + Express para demonstração, com armazenamento em memória (os dados são perdidos ao reiniciar o servidor).",
  "main": "index.js",
  "scripts": {
     "test": "jest --forceExit --detectOpenHandles",
     "dev": "node --watch src/server.js",
     "start" : "node src/server.js"
  },
   "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
